export type NavItem = {
  title: string
  icon: string
  path: string
  id: string
}

export type BaseButton = {
  title: string
  path: string
}

export type SocialItem = {
  title: string
  icon: string
  path: string
}
